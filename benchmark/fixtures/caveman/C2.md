Add due_date to tasks end-to-end:
1. Model: add `due_date: Optional[str] = None` to Task dataclass in taskflow/models/task.py. BaseModel.to_dict use asdict → serialize auto.
2. Service: extend TaskService.create_task in taskflow/services/task_service.py to take due_date, pass to Task(...). Optional: add helper query overdue tasks via TaskRepository.
3. Repository: no storage change (BaseRepository.add serialize all fields). May add list_overdue(now) to taskflow/repositories/task_repo.py.
4. API: read due_date from body in create_task handler in taskflow/api/routes_tasks.py, forward to service.
5. Tests: add case to tests/test_task_service.py — assert due_date round-trips through create_task, present in task dict.
No DB schema change. Database stores arbitrary row dicts.