```python
# taskflow/models/task.py
@dataclass
class Task(BaseModel):
    title: str = ""
    description: str = ""
    status: Status = Status.TODO
    project_id: Optional[int] = None
    assignee_id: Optional[int] = None
    due_date: Optional[str] = None  # ponytail: opaque ISO string, stored & returned only. Not parsed/validated — nothing consumes it as a date yet; add validation when something does.
```

```python
# taskflow/services/task_service.py
    def create_task(
        self, title: str, project_id: int, assignee_id: Optional[int] = None,
        due_date: Optional[str] = None,
    ) -> Task:
        task = self.tasks.add(Task(title=title, project_id=project_id,
                                   assignee_id=assignee_id, due_date=due_date))
        if assignee_id is not None:
            self.notifications.notify(assignee_id, f"Assigned task {task.id}")
        return task
```

```python
# taskflow/api/routes_tasks.py
    def create_task(app: App, body: dict) -> dict:
        task = app.services.tasks.create_task(
            title=body.get("title", ""),
            project_id=body.get("project_id"),
            assignee_id=body.get("assignee_id"),
            due_date=body.get("due_date"),
        )
        return {"status": 201, "task": task.to_dict()}
```

```python
# tests/test_task_service.py
def test_create_task_with_due_date():
    services = build_services()
    task = services.tasks.create_task("Write report", project_id=1, due_date="2099-01-01")
    assert task.due_date == "2099-01-01"
    assert task.to_dict()["due_date"] == "2099-01-01"
```